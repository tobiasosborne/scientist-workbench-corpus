[adapter]
target          = "scientist-workbench"
capability_id   = "groebner-basis"
cmd             = "bun"
args            = ["run", "${SUITE_ROOT}/run-candidate.ts"]
cwd             = "${WORKBENCH_ROOT}"
version         = "phase-2a-spec (tool not yet implemented)"
platform_pinned = false
