[adapter]
target          = "scientist-workbench"
capability_id   = "integrate-ode-symplectic"
cmd             = "bun"
args            = ["run", "${SUITE_ROOT}/run-candidate.ts"]
cwd             = "${WORKBENCH_ROOT}"
version         = "git@be0e613"
platform_pinned = true
