[adapter]
target          = "scientist-workbench"
capability_id   = "integrate-ode-stiff"
cmd             = "bun"
args            = ["run", "${SUITE_ROOT}/run-candidate.ts"]
cwd             = "${WORKBENCH_ROOT}"
version         = "git@e27eb33"
platform_pinned = true
