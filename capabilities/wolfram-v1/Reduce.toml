# Auto-generated by scripts/ingest_wolfram_v1.ts on 2026-05-06.
# Source: data/wolfram-v1/raw/contents/list427.pdf.
#
# Round-trip discipline: re-running the ingestor regenerates [meta], [signature],
# [description], and the primary [[provenance]] row from the PDF. Hand-edits to
# [category], [verification], [[mapping]], and any additional [[provenance]]
# rows are preserved across re-ingestion.
# See also (cross-refs from PDF — candidates for cross-system aliases):
#   Solve, Eliminate, LogicalExpand, ToRules
# Back-reference: page 401 of the 2nd edition.

[meta]
system      = "mathematica"
version     = "1"
name        = "Reduce"
section     = "ref-guide:list427"
ingested_at = "2026-05-06"
ingested_by = "scripts/ingest_wolfram_v1.ts"

[signature]
input  = "any"
output = "any"
arity  = 1

[description]
md = '''
Reduce[eqns, vars] simplifies the equations eqns, attempting to solve for the variables vars. The equations generated by Reduce[are equivalent to eqns, and contain all the possible solutions. Reduce eqns, vars, elims] simplifies the equations, trying to eliminate the variables elims. The equations given to Reduce[are in the form lhs == rhs. Simultaneous equations can either be given in a list, or b combined with &&. Example: Reducea x + b == 0, x] ;;! a != 0 && x == -(-) || a == 0 && b == 0. a Reduce generates equations (==) and nonequalities (!=), combined with && and ||. Reduce primarily deals with polynomial equations. You can give options to Reduce, as described in the notes on MainSolve.
'''

[category]
primary   = "algebra"
secondary = ["polynomial", "symbolic"]

[[provenance]]
field_path   = "description.md"
value        = "Reduce[eqns, vars] simplifies the equations eqns, attempting to solve for the variables vars. The equations generated by Reduce[are equivalent to eqns, and contain all the possible solutions. Reduce e..."
source_url   = "https://reference.wolfram.com/legacy/v1/contents/list427.pdf"
source_kind  = "scrape"
retrieved_at = "2026-05-06"
local_path   = "data/wolfram-v1/raw/contents/list427.pdf"
sha256       = "4773b1fcae671daeebe61a81c12b34de24c3d01287248fb87e8e2ce04e2b610f"

[verification]
level             = 1
method            = "manual_quote"
last_verified_at  = "2026-05-06"
